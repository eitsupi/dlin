name: simple_project
version: '1.0.0'
config-version: 2

model-paths: ["models"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]

vars:
  payment_methods: ["credit_card", "bank_transfer"]
  order_sources: ["online", "retail"]
