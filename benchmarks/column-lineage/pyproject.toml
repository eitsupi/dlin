[project]
name = "dlin-column-lineage-correctness-fixtures"
version = "0.1.0"
description = "Small synthetic dbt-duckdb fixtures for column-lineage correctness checks"
requires-python = ">=3.12"
dependencies = [
    "dbt-core==1.12.2",
    "dbt-duckdb==1.11.0",
]

[tool.uv]
package = false
